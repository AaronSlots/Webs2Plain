<div class="card">
    <div class="card-header">{{ __('cards.new') }}</div>
    <div class="card-body">
        <a href="/card/new" class="btn btn-primary">{{ __('cards.new_link') }}</a>
    </div>
</div>